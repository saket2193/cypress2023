let a="goodevening"
console.log(a.length) //length of string

console.log(a.slice(0,4)) //slicing a string

console.log(a[3])// printing based on the index

console.log(a.split("o")) //Split a string into substrings using the specified separator and return them as an array.

console.log(a.trim()) //Removes the leading and trailing white space and line terminator characters from a string.


//find the difference between below two strings
b='23'
c='28'

diff=parseInt(c)-parseInt(b) //parseInt converts a string to an integer.
console.log(diff.toString())
console.log(typeof(diff))
